/* ============================================================
   config.js — YOUR settings. This is the only file you edit by hand.
   ============================================================ */

window.MM_CONFIG = {

  /* — PASSWORD —
     The default password is:  manifest
     To change it:
       1. Open the app in your browser.
       2. Open the console (right-click → Inspect → Console).
       3. Run:   await mmHash('your-new-password')
       4. Copy the long hash it prints and paste it below,
          replacing the value of passwordHash.
       5. Save, commit, push. Done.                                  */
  passwordHash: '05b3abf2579a5eb66403cd78be557fd860633a1fe2103c7642030defe32c657f', // "manifest"

  /* — Which week is the brief data for? Display only. —
     Cowork updates data/brief.json every Friday; this is a fallback label. */
  appName: 'Manifesting Me',
  tagline: 'She is already her. This confirms it.'
};
